# Contract: Agent 局部关卡修改

1. 读取完整关卡集，只调整用户指定关卡。
2. 保留其他关卡、未指定对象和现有能力。
3. 只对已支持对象直接修改 `src/levels.json`，不修改兼容文件 `src/level.json`。
4. 不重新生成 GDD、素材或无关场景，不修改固定运行文件注入障碍。
5. 写入前保留完整原内容，写入后执行关卡解析、构建和必要测试。
6. 失败时停止，不把变化转移到其他文件。
