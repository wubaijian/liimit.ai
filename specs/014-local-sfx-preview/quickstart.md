# Quickstart: 本地音效试听

## 重新生成

```bash
node scripts/generate-local-sfx-preview.mjs
```

## 试听

用浏览器打开：

```text
preview-assets/local-sfx/index.html
```

## 验收边界

本页只用于选择声音风格。试听文件不会自动进入游戏，也不会绑定动作；用户确认后再开始下一部分。
