# Tasks: 横版试玩内置本地音效

- [x] T001 确认六种现有事件与当前演示项目定制代码位置
- [x] T002 添加本地音效配置、音量和防重复规则
- [x] T003 将六个 WAV 打包进固定模板
- [x] T004 在固定模板场景中加载并绑定六个事件
- [x] T005 保留森林美术并同步当前 `星光森林` 项目
- [x] T006 添加配置单测、模板契约和资源完整性检查
- [x] T007 运行模板、演示项目和桌面端回归
- [x] T008 在应用内 Web 试玩中确认资源可加载且游戏无异常

## Final Evidence (2026-09-01)

- 固定模板包含六个与已确认试听文件 SHA-256 完全一致的 WAV，且本地配置不含 URL、API Key 或网络调用。
- 六个事件均通过统一的 `playLocalSfx` 安全入口播放；浏览器声音锁定、缓存缺失或单次播放异常都会安全跳过。
- 模板完整新建流程通过：无模型配置、无 Agent、生成式 AI 调用 0 次；项目构建成功，14 项模板测试通过，其中包括 2 项音效规则测试。
- 当前 `星光森林` 保留森林美术并通过 typecheck、11 项游戏测试和正式构建；构建产物包含六个有效 WAV。
- 当前项目改动前的场景文件备份在 `.gameagent/backups/2026-09-01-before-local-sfx/`，可以恢复。
- 桌面端完整回归：52 个测试文件、626 项测试全部通过；桌面正式构建通过。
- 应用内 Web 试玩刷新成功，无可见错误；六个本地声音地址均返回 HTTP 200、`audio/wav` 和正确文件大小。手动触发一次跳跃后游戏继续运行。
