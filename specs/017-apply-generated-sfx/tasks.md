# Tasks: 将生成音效应用到当前游戏

- [x] T001 明确六种用途、用户确认、固定路径和失败回滚边界
- [x] T002 扩展生成接口为受信任的六用途映射
- [x] T003 增加项目音效覆盖配置并让模板安全读取
- [x] T004 实现受限项目写入、构建和失败回滚服务
- [x] T005 增加强类型 IPC、运行时校验和应用前确认
- [x] T006 在音频设置中增加用途选择、试听和应用按钮
- [x] T007 保留森林美术并同步当前 `星光森林`
- [x] T008 添加服务、IPC、模板和界面测试
- [x] T009 运行固定模板、当前项目与桌面端完整回归
- [x] T010 在应用内检查界面与无额度应用验证

## Final Evidence (2026-09-02)

- 音效生成现在只接受六种固定用途，Main 为每种用途映射不同的内置描述；Renderer 不能提交 API Key、Base URL、任意提示词或目标文件路径。
- 试听结果携带用途、模型、MP3 字节和耗时；界面切换用途时会释放旧试听，避免把错误声音应用到另一个动作。
- “应用到当前游戏”使用独立强类型 IPC。Main 校验项目 ID、用途和字节，显示项目名称与替换用途，用户取消时返回 `cancelled`，不写文件。
- 项目音效服务只写 `public/assets/audio/custom/` 和 `src/audioOverrides.json`；拒绝超过 1 MiB、非 MP3、非固定项目和符号链接越界。成功后重新构建，构建失败恢复旧配置与旧音频。
- 模板在覆盖为空时继续使用六个已验收 WAV；合法覆盖只对对应的一种声音改用固定 MP3。新建模板真实冒烟通过，15 项模板测试通过，生成式 AI 调用 0 次。
- 当前 `星光森林` 只新增覆盖 JSON 并调整声音路径解析，森林场景、美术和关卡未修改；通过 typecheck、11 项测试和正式构建。修改前 `localSfx.ts` 已保存在 `.gameagent/backups/2026-09-02-before-generated-sfx-apply/`。
- 桌面端完整回归：53 个测试文件、634 项测试全部通过；桌面 TypeScript 检查和正式构建通过。
- 应用已重启并实际显示六种用途下拉框，布局无重叠。为避免消耗用户 ElevenLabs 额度，没有点击真实生成；成功写入、构建、失败回滚和越界拒绝均在隔离临时项目中验证。
