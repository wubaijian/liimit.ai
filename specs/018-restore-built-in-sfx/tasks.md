# Tasks: 一键恢复内置音效

- [x] T001 明确单项恢复、确认、保留 MP3 和失败回滚边界
- [x] T002 扩展共享类型与 Preload 契约
- [x] T003 增加覆盖状态检查和单项恢复服务
- [x] T004 增加 Main 运行时校验、确认与 IPC
- [x] T005 在音频设置中显示来源状态和恢复按钮
- [x] T006 添加服务、契约和界面测试
- [x] T007 运行固定模板、当前项目和桌面端完整回归
- [x] T008 在应用内检查真实状态和布局

## Final Evidence (2026-09-02)

- 新增只读状态接口，只返回项目 ID 和六个布尔值；Renderer 不接触绝对路径、配置文件内容或自定义 MP3 字节。
- 恢复接口只接受项目 ID 与六种固定用途，Main 在写入前显示项目名称和声音用途；取消、已是内置、确认恢复三种结果明确。
- 服务测试覆盖六项状态读取、单项恢复、不删除 MP3、无覆盖不构建、构建失败恢复原配置，以及此前的路径/格式/大小保护。
- 界面在选择用途后显示“当前使用：内置音效 / AI 自定义音效”；只有自定义状态才启用“恢复内置音效”。项目切换和连续刷新使用请求编号避免旧结果覆盖新项目。
- 固定模板真实新建冒烟通过，15 项模板测试通过，生成式 AI 调用 0 次。当前 `星光森林` 未发生任何文件修改，继续使用内置音效。
- 桌面端完整回归：53 个测试文件、639 项测试全部通过；桌面 TypeScript 检查和正式构建通过。
- 应用重启后实际读取 `星光森林` 跳跃声音为“内置音效”，恢复按钮正确禁用，界面布局无重叠；未调用 API、未写项目文件。
