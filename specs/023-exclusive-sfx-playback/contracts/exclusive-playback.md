# Contract: 候选音效单条试听

- 每个候选播放器以稳定候选编号登记。
- 播放事件触发时，只暂停并重置编号不同的播放器。
- 原生暂停不重置当前播放器，因此再次播放可以继续。
- “从头播放”先暂停/重置其他播放器，再把目标设为 0 并调用一次播放。
- 播放 Promise 失败只产生本地错误提示。
- 卸载播放器时暂停、归零并移除引用。
- 契约不包含 API、IPC、密钥、项目或文件操作。
