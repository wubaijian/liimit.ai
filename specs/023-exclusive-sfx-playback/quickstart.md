# Quickstart: 候选音效单条试听控制

## 自动检查（不调用 API）

1. 三个本地假播放器依次播放，确认只有目标候选保留且其他候选暂停归零。
2. 当前候选暂停后位置不被重置，继续播放仍从原位置开始。
3. 从头播放确认目标位置设为 0，只调用一次播放。
4. 候选移除确认播放器暂停、归零且引用删除。
5. 运行聚焦测试、两个 TypeScript 检查、完整回归、正式构建和差异检查。

## Electron 界面检查

打开设置 → AUDIO，只确认静态说明和已有候选状态契约；不点击生成，不调用 ElevenLabs。
