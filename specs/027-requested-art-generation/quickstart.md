# Validation

运行桌面测试和 package:mac，安装重启检查进程。覆盖内容修改、删除、相同重写、链接与缺失目录。真实图片生成需用户配置支持出图的服务，本次不自动消费 API。

## 实际验证 2026-09-14

60 个测试文件、722 项测试通过；renderer/main 类型检查通过；构建、隔离运行时和固定游戏冒烟测试通过；arm64 DMG 安装包验证通过。本地未签名构建，未做公证或干净机器公开发行验证。
已安装 /Applications/liimit.ai.app，关闭旧进程后重新启动；内置 custom.md 确认为 V2.1，界面正常显示9个现有项目。没有修改历史完成记录。未发起付费图像或主模型任务，因此不能声称真实图片端到端生成通过。
