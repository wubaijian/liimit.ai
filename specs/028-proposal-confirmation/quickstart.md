# Validation

运行桌面测试、类型检查、package:mac。已存在中文确认方案在重启后显示卡片；确认与调整通过mock runner验证生成指令，取消通过真实临时事件存储验证。线上付费模型本次不运行。

## 2026-09-14 验证结果

- 桌面全部 62 个测试文件、737 项测试通过；包含真实临时事件存储关闭重开后的取消恢复检查。
- renderer/main 类型检查通过；package:mac 完成，隔离运行时检查、无 API 固定模板制作与试玩检查、macOS DMG 验证通过。
- 已安装到 /Applications/liimit.ai.app；旧应用移至废纸篓 liimit-ai-before-proposal-card-20260914.app，可恢复。未删除游戏与设置。
- 原生应用实际显示“小冒险”历史方案及确认修改、调整方案、取消三个按钮。实点调整方案后输入框展开、空输入时提交禁用、返回方案正常。未提交真实方案、未调用付费模型、未修改用户关卡。
- 安装包为本机未签名构建，不视为已完成对外分发签名/公证。

## 收敛核对

FR-001 由共享解析器与卡片覆盖；FR-002 由持久历史校验及可信 IPC 覆盖；FR-003 由互斥锁、取消事件及重开存储测试覆盖；FR-004 由续会话指令与失败重试测试覆盖。任务、契约、实现一致，无未完成必需任务。真实模型遵循方案与实际文件修改效果仍需用户点击确认后验收，本次不冒充线上验证。
