# Contracts

AI 创建与模板入口分离，不改变已有 level schema；基础文件包含兼容出生点/平台/终点以保留编辑、构建、预览契约，UI 标明待创建。
Agent 受控环境 GAME_STARTER_TEMPLATE_ID=ai-foundation 时 classify 只检查当前基础，不复制示例。
custom 渲染不加载内置火山图片，不启动特定 id 的巨兽；可用 visualStyle 配色或自有图片。原有模式按默认 builtin 渲染。
初次完成需关卡解析通过、布局或能力实际变化、非占位命名，再通过构建及本地入口验证。停止和失败沿用现有状态。
