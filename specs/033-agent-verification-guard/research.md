# Research

- 决策：辅助文件指引使用项目内 .liimit-checks，保留原权限；不扩大工作区或用 shell 绕过。
- 决策：复用 verifyPlayableBuild 预览服务核验 JSON；禁止模型另启 Vite 检查。/\_\_liimit/\* 由桌面提供，单独 Vite 返回 HTML。
- 决策：独立 Guard 聚合错误和构建后预算，Runner 终止进程树。仅提示词无法制止循环。已有无输出超时保留；素材在途暂缓预算停止。
- 决策：每轮按 tool_use_id 去重，同类错误跨普通成功读取累计；只对命令输出检测栈、退出码等失败特征，不因源代码含 Error 字样误判。
- 未知项已解决，无外部研究或新依赖需求。
